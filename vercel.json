{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "express",
  "buildCommand": "npm run verify && npm run build",
  "functions": {
    "server.ts": {
      "includeFiles": "{dist/ui/**,dist/site/**,icon/icon.png}",
      "maxDuration": 60
    }
  }
}
